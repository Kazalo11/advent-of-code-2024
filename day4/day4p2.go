package main

var mas = [][2]string{
	{"M", "S"},
	{"S", "M"},
	{"M", "M"},
	{"S", "S"},
}

func Part2() {
	matrix := getData()
	for rowIndex, row := range matrix {
		for column := range row {
			path := make([]Coordinate, 0)
		}
	}
}
